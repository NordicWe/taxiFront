// Энэ файл Photon руу шилжих явцад хэрэггүй болсон.
// Аппд import хийгдэхгүй болсон тул build-д орохгүй (tree-shaken).
// Файлыг устгаж болно — энэ нь зөвхөн dead-code stub.

export function loadGoogleMaps(): Promise<void> {
  return Promise.resolve();
}
